[project]
name = "kidquest-fh2m"
version = "0.0.0"
description = "Read/patch/write the fheroes2 .fh2m map format"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["kidquest_fh2m"]
