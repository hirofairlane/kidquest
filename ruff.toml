# Code-quality gate for the Python packages (server + content pipeline).
target-version = "py311"
line-length = 100
extend-exclude = ["shared/py", "_reference", "scripts"]

[lint]
select = ["E4", "E7", "E9", "F", "I"]  # pyflakes + import sorting + syntax
